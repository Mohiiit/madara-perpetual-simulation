{
  "name": "madara-perpetual-simulation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Madara devnet + Starknet Perpetual simulation harness",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "simulate": "tsx src/main.ts",
    "simulate:dist": "node dist/main.js"
  },
  "dependencies": {
    "starknet": "^9.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.2"
  }
}
